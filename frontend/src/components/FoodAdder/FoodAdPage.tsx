import {useContext} from "react";
import FoodContext from "./FoodContext";

export function FoodAdPage() {
    const {currentFood} = useContext(FoodContext)
    return (
        <>

        </>
    )
}